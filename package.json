{
  "name": "gogov",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm --workspace @gogov/web run dev",
    "dev:api": "npm --workspace @gogov/api run dev",
    "dev": "concurrently -n web,api -c blue,green \"npm run dev:web\" \"npm run dev:api\"",
    "build": "npm --workspace @gogov/web run build && npm --workspace @gogov/api run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
