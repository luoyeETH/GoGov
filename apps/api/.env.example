API_PORT=3001
DATABASE_URL=postgresql://gogov:gogov@localhost:5432/gogov
APP_BASE_URL=https://app.example.com
EMAIL_FROM=学了么 <no-reply@example.com>
RESEND_API_KEY=
SESSION_TTL_DAYS=7
PASSWORD_SALT_ROUNDS=12

# Optional: enable strict CORS by listing allowed origins (comma-separated).
# Example: CORS_ORIGINS=https://app.example.com,http://localhost:3030
# CORS_ORIGINS=

# Optional: trust proxy hop count for correct request.ip / rate limit behind nginx/caddy.
# Example: TRUST_PROXY=1
# TRUST_PROXY=
